import type { POI } from '../../types/poi';
export function TourOverview({stops,onStart,onClose,language='en'}:{stops:POI[];onStart:()=>void;onClose:()=>void;language?:string}) {return <section className="tour-overview"><header><div><small>{language==='es'?'PASEO INTELIGENTE':'SMART WALK'}</small><h2>{language==='es'?'Tour de 30 minutos':'30-minute tour'}</h2></div><button onClick={onClose}>×</button></header><p className="tour-summary">{stops.length} {language==='es'?'paradas · unos 30 minutos · a tu ritmo':'stops · about 30 minutes · self-paced'}</p><ol>{stops.map((p,i)=><li key={p.id}><span>{i+1}</span><div><strong>{p.emoji} {language==='es'?(p.nameEs??p.name):p.name}</strong><small>{p.category}</small></div></li>)}</ol><button className="tour-start" onClick={onStart}>{language==='es'?'Comenzar tour':'Start tour'}</button></section>;}